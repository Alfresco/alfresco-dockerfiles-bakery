# Sync override example.
artifacts:
  sync-distribution:
    name: sync-dist-6.x
    version: 5.3.6
    classifier: ".zip"
    group: org.alfresco.services.sync
    repository: enterprise-releases
    path: sync/distribution
